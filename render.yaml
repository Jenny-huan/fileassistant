services:
  - type: web
    name: document-image-organizer
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.server --host 0.0.0.0
    healthCheckPath: /health
